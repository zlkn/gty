package main

// baseDPI is what a point is worth before the display's scale is applied. macOS counts a
// point as a pixel, where every other platform inherited Windows' 4/3 logical inch.
const baseDPI = 72.0
